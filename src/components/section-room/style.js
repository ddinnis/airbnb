import styled from "styled-components";

export const SectionRoomWrapper = styled.div`
  margin: 24px 0;
  .room-list {
    display: flex;
    flex-wrap: wrap;

    /* 间距为16px */
    margin: 0 -8px;
  }
`;
